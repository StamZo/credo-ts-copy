import type { VersionString } from '../../utils/version';
import type { UpdateToVersion } from './updates';
export declare function isStorageUpToDate(storageVersion: VersionString, updateToVersion?: UpdateToVersion): boolean;
